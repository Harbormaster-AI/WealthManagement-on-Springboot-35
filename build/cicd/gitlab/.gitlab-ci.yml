



stages:
  - build-and-test

before_script:


build:
  stage: build-and-test
  script:
    - cd terraform && chmod 777 terraform.sh && ./terraform.sh install


  artifacts:
    paths:
      - WealthManagementonspringboot35binary
      